function mainFunctionTask1()
{
    let name = document.querySelectorAll('input[type=text]')[0].value;
    console.log(name);

    let phone = document.querySelectorAll('input[type=phone]')[0].value;
    console.log(phone);

    let dob = document.querySelectorAll('input[type=date]')[0].value;
    console.log(dob);

    let email = document.querySelectorAll('input[type=email]')[0].value;
    console.log(email);
}